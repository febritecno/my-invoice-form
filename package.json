{
  "name": "monorepo-launcher",
  "version": "1.0.0",
  "description": "Monorepo containing frontend and backend applications",
  "author": "Febrian Dwi Putra",
  "scripts": {
    "install": "npm install && npm-run-all --parallel install:frontend install:backend",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "dev": "npm-run-all --parallel dev:frontend dev:backend",
    "dev:frontend": "cd frontend && npm run start",
    "dev:backend": "cd backend && npm run server",
    "build:frontend": "cd frontend && npm run build",
    "start": "npm run install && npm run dev",
    "prod": "npm run install && npm-run-all --parallel dev:backend build:frontend"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "npm-run-all": "^4.1.5"
  }
}